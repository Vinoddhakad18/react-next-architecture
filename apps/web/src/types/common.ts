/**
 * Common Type Definitions
 */

// Add shared/common types here that don't belong to a specific domain.
// `export {}` marks this file as a module so it can be safely re-exported.
export {};






